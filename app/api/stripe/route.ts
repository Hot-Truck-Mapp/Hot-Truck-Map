import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({ message: "Stripe API coming soon" });
}

export async function POST() {
  return NextResponse.json({ message: "Stripe API coming soon" });
}